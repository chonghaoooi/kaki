# Example only. Never commit a populated .env file.
# Use Node's --env-file=.env option; the runner does not auto-load dotenv files.
KAKI_DEMO_MODE=false
PORT=4173
KAKI_ORIGIN=http://127.0.0.1:4173
KAKI_DB_PATH=./data/kaki-live.sqlite
RESEND_API_KEY=
MAIL_FROM=
# Optional server-only OneMap token for exact postal lookup beyond public demo fixtures.
ONEMAP_TOKEN=
